<!-- This page is the guide the engine itself ships and serves: the file lives
     in `crates/lumina-core/src/authoring_guide.md` so it is versioned with the
     schema, reachable as `GET /guide`, `lumina-cli guide` and the
     `lumina_guide` tool, and included here rather than copied. -->

{{#include ../../crates/lumina-core/src/authoring_guide.md}}
