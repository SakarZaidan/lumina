[workspace]
# Kept out of the main workspace on purpose: libfuzzer-sys requires a nightly
# toolchain and sanitizer flags, so `cargo check --workspace` must not see it.

[package]
name = "lumina-fuzz"
version = "0.0.0"
edition = "2021"
publish = false
license = "MIT"
description = "Fuzz targets for Lumina's untrusted-input parsers."

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
lumina-core = { path = "../crates/lumina-core" }
lumina-schema = { path = "../crates/lumina-schema" }
lumina-renderer = { path = "../crates/lumina-renderer" }
serde_json = "1"

[[bin]]
name = "scene_json"
path = "fuzz_targets/scene_json.rs"
test = false
doc = false
bench = false

[[bin]]
name = "svg_path"
path = "fuzz_targets/svg_path.rs"
test = false
doc = false
bench = false

[[bin]]
name = "latex_unicode"
path = "fuzz_targets/latex_unicode.rs"
test = false
doc = false
bench = false

[[bin]]
name = "plot_expression"
path = "fuzz_targets/plot_expression.rs"
test = false
doc = false
bench = false
